import COLORS from "./colors";

export const Colors = {
  light: {
    text: COLORS.BLACK,
    background: COLORS.WHITE,
    tint: COLORS.PRIMARY,
    error: COLORS.RED,
    success: COLORS.GREEN,
    border: COLORS.GRAY,
  },
  dark: {
    text: COLORS.WHITE,
    background: COLORS.BLACK,
    tint: COLORS.PRIMARY,
    error: COLORS.RED,
    success: COLORS.GREEN,
    border: COLORS.GRAY,
  },
};
