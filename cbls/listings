// screens/SearchScreen.tsx
import React from 'react';
import { View, Text, TextInput, ScrollView, TouchableOpacity, Image } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { MagnifyingGlass, SlidersHorizontal, Clock, MapTrifold } from 'phosphor-react-native';

export default function SearchScreen() {
  const tabs = ['All', 'Apartment', 'Rent', 'Land'];
  const recentSearches = ['Lekki Phase 1', 'Victoria Island', 'Ikoyi'];
  
  return (
    <SafeAreaView className="flex-1 bg-white" edges={['top']}>
      <ScrollView className="flex-1 px-4 pt-2">
        
        {/* Custom Search Input bar */}
        <View className="flex-row items-center bg-slate-50 border border-slate-200 rounded-2xl px-3 py-2.5 mb-4">
          <MagnifyingGlass size={20} color="#64748B" className="mr-2" />
          <TextInput 
            placeholder="Search location, property..." 
            placeholderTextColor="#94A3B8"
            className="flex-1 text-sm text-darkText font-normal p-0"
          />
          <TouchableOpacity className="p-1">
            <SlidersHorizontal size={20} color="#0066FF" />
          </TouchableOpacity>
        </View>

        {/* Category horizontal scroll selection */}
        <ScrollView horizontal showsHorizontalScrollIndicator={false} className="flex-row mb-6">
          {tabs.map((tab, idx) => (
            <TouchableOpacity 
              key={idx} 
              className={`px-5 py-2 rounded-full mr-2 border ${idx === 0 ? 'bg-primary border-primary' : 'bg-white border-slate-200'}`}
            >
              <Text className={`text-xs font-medium ${idx === 0 ? 'text-white' : 'text-mutedText'}`}>{tab}</Text>
            </TouchableOpacity>
          ))}
        </ScrollView>

        {/* Recent Search History Section */}
        <View className="mb-6">
          <View className="flex-row justify-between items-center mb-3">
            <Text className="text-sm font-bold text-darkText">Recent Searches</Text>
            <TouchableOpacity><Text className="text-xs text-primary font-medium">Clear All</Text></TouchableOpacity>
          </View>
          {recentSearches.map((item, idx) => (
            <View key={idx} className="flex-row items-center justify-between py-3 border-b border-slate-50">
              <View className="flex-row items-center space-x-3">
                <Clock size={18} color="#94A3B8" />
                <Text className="text-sm text-darkText font-medium">{item}</Text>
              </View>
            </View>
          ))}
        </View>

        {/* Popular Locations grid layouts */}
        <View className="mb-6">
          <Text className="text-sm font-bold text-darkText mb-3">Popular Locations</Text>
          <View className="flex-row justify-between">
            <LocationCard name="Lekki" image="https://images.unsplash.com/photo-1542314831-068cd1dbfeeb" />
            <LocationCard name="VI" image="https://images.unsplash.com/photo-1570129477492-45c003edd2be" />
            <LocationCard name="Ikoyi" image="https://images.unsplash.com/photo-1580587771525-78b9dba3b914" />
          </View>
        </View>

        {/* Exclusive Feature Map Banner Card */}
        <View className="bg-gradient-to-r from-blue-600 to-blue-500 rounded-2xl overflow-hidden relative p-5 mb-8 min-h-[140px] bg-primary justify-between">
          <View className="z-10 w-2/3">
            <Text className="text-[10px] uppercase font-bold tracking-widest text-blue-200 mb-1">Exclusive Feature</Text>
            <Text className="text-lg font-extrabold text-white leading-tight">Find Off-Market Deals</Text>
            <Text className="text-xs text-blue-100 mt-1">Connect directly with top owners</Text>
          </View>
          <TouchableOpacity className="bg-white/20 backdrop-blur-md self-start flex-row items-center px-4 py-2 rounded-xl mt-3 space-x-2">
            <MapTrifold size={16} color="#FFFFFF" weight="bold" />
            <Text className="text-white text-xs font-semibold">Map View</Text>
          </TouchableOpacity>
        </View>

      </ScrollView>
    </SafeAreaView>
  );
}

function LocationCard({ name, image }: { name: string, image: string }) {
  return (
    <View className="w-[30%] items-center">
      <Image source={{ uri: image }} className="w-full h-20 rounded-xl" />
      <Text className="text-xs font-semibold text-darkText mt-2">{name}</Text>
    </View>
  );
}